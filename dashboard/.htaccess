# NOTE:
# This project is currently hosted under /dashboard/estudiantes_intranet/ in XAMPP.
# Having an extra nested /dashboard/ directory can cause confusing redirects on phones.
# We keep this folder as a simple alias that redirects back to the real app files.

RewriteEngine On

# Redirect everything in this folder to the parent project directory.
RewriteRule ^$ ../dashboard.php [R=302,L]
RewriteRule ^(.+)$ ../$1 [R=302,L]
